/**
 * Client side configuration object. Can be used to store global configuration values.
 */
export default {
    JWT_TOKEN: 'jwt-token',
    CURRENT_USER_ID: 'current_user_id',
    API_BASE_URL: '/api'
}
